function Add({ a, b }) {
    return (
        <div>
            <h2>Add</h2>
            <p className="text-black">
                {a} + {b} = {a + b}
            </p>
        </div>
    );
}
export default Add;